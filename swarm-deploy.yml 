version: '3.8'
services:
  ecommerce-app:
    image: your-dockerhub-user/ecommerce-app:1.0
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        parallelism: 2
        failure_action: rollback
    ports:
      - "8081:8080"
